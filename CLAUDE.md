# Prevail Project Notes

## Database rules

- ทุกครั้งที่ต้องแก้ไขโครงสร้างฐานข้อมูล (SQL) บน localhost เช่น CREATE/ALTER/DROP TABLE, เพิ่ม/ลบ/แก้ไข column, index, constraint ฯลฯ ห้ามรันคำสั่งหรือประมวลผลเอง
- ให้เขียนเป็น SQL query ส่งให้ผู้ใช้ไปรันเองเท่านั้น
